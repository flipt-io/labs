# Enabling the chat-bot

In the `basic` tutorial we defined the feature flag `chat-enabled`.

Currently, our git repository contains a definition for this flag, however, it is in a `disabled` state:

![Screenshot of features configuration file with disabled flag](/images/gitops/disabled-flag.png)

Our helpful colleague has prepared a pull-request which enables this flag.

![Screenshot of pull-request](/images/gitops/pr-one.png)

Head over to [pull request 1](http://localhost:3001/flipt/features/pulls/1) to see the contribution.

Click 'Next' to continue.
